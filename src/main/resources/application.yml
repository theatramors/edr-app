server:
  port: 10001

spring:
  profiles:
    active: develop, test
  main:
    banner-mode: 'off'

---

spring:
  profiles: develop
  main:
    banner-mode: 'off'

---

spring:
  profiles: test
  main:
    banner-mode: 'off'
